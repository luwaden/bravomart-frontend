# Copy this to .env and adjust if your backend runs somewhere other than
# localhost:4000. Vite only exposes variables prefixed with VITE_ to the
# browser (see src/services/api.js).
VITE_API_BASE_URL=http://localhost:4000/api
